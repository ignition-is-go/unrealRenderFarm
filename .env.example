# Render Farm Configuration - copy to .env

# Server URL (used by workers and submitters)
RENDER_SERVER_URL="http://192.168.1.158:5000"

# Server configuration (requestManager.py only)
RENDER_SERVER_HOST="0.0.0.0"
RENDER_SERVER_PORT="5000"
FLASK_DEBUG="false"  # NEVER set to true in production!

# Render nodes only (use forward slashes on Windows)
UNREAL_EXE="C:/Program Files/Epic Games/UE_5.4/Engine/Binaries/Win64/UnrealEditor.exe"
UNREAL_PROJECT="D:/Projects/YourProject/YourProject.uproject"
# WORKER_NAME="render-node-01"  # Optional, defaults to hostname
